·       Ability to select the date, month, year from the calendar.·       Ability to open the calendar.·       Ability to enter the dates manually.·       Ability to select the month from the drop down menu.·       Date should display as per selection done by the user.·       Drop down menu should be selectable to select the month.·       Once date and year is selected, calendar should be closed automatically.

public class SelectingDates {
    
    Date selectedDate;
    Date currentDate;
    Date startDate;
    Date endDate;
    
    public void openCalendar() {
        // code to open calendar
    }
    
    public void setDate() {
        // code to set date
    }
    
    public void selectMonth() {
        // code to select month
    }
    
    public void enterDateManually() {
        // code to enter date manually
    }
    
    public void selectYear() {
        // code to select year
    }
    
    public void closeCalendar() {
        // code to close calendar
    }
    
    public void displaySelectedDate() {
        // code to display selected date
    }
    
    public void getDataFromStartToEndDate() {
        // code to get data from start to end date
    }
}